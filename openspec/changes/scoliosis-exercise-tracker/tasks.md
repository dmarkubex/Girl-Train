## 1. 项目初始化与基础架构

- [x] 1.1 初始化 Vite + TypeScript 项目，配置 `tsconfig.json` 和构建脚本
- [x] 1.2 搭建项目目录结构（`src/`, `views/`, `timer/`, `utils/`, `components/`, `styles/`）
- [ ] 1.3 实现 Hash 路由器（`router.ts`）— 支持页面切换和 URL 映射
- [ ] 1.4 实现简单的全局状态管理（`state.ts`）— pub/sub 模式
- [ ] 1.5 创建基础 CSS 变量和全局样式（`styles/variables.css`, `styles/main.css`）— 移动端优先的响应式布局
- [ ] 1.6 实现业务日期工具模块（`utils/date.ts`）— `getBusinessDate()` 函数，凌晨 4:00 作为日分界线

## 2. 数据层（IndexedDB）

- [ ] 2.1 安装 `idb` 依赖，创建数据库初始化模块（`db.ts`）— 定义 `config`、`sessions` 和 `metadata` 三个 object store
- [ ] 2.1.1 实现 IndexedDB 版本迁移框架 — `DB_VERSION` 常量 + `migrations[]` 数组，`upgrade` 回调中按序执行 `migrations[oldVersion..newVersion-1]`；V1 migration 创建三个 store
- [ ] 2.2 定义会话数据结构 — `Session` 包含 `exercises: Array<{ name, sets: Array<{ planned: number, actual: number, skipped: boolean }> }>`，每组记录计划时长与实际执行时长
- [ ] 2.3 实现锻炼配置的 CRUD 操作（读取/保存锻炼计划配置）
- [ ] 2.4 实现锻炼记录的存储和查询（按业务日期范围查询、按日期降序排列，使用 `getBusinessDate()` 归档）
- [ ] 2.5 实现默认锻炼计划的初始化逻辑（首次使用时自动创建默认配置：抱头展胸 70s/15s/9组、上胸弯对抗 70s/15s/9组等）
- [ ] 2.6 实现数据导出（JSON 文件下载，包含 config、sessions、metadata）和导入功能（文件选择 + JSON schema 验证）
- [ ] 2.6.1 实现导入合并策略 — sessions 按 `sessionId` 去重（跳过已存在记录）；config 提示用户选择保留本地或覆盖；metadata 取较大值合并
- [ ] 2.7 创建 `metadata` object store，实现 `maxStreak`（历史最长连续天数）的读写操作
- [ ] 2.8 实现 streak 计算逻辑 — 每次保存锻炼记录后重新计算当前 streak，如超过 `maxStreak` 则更新

## 3. 计时器引擎

- [ ] 3.1 实现计时器状态机（`timer/engine.ts`）— 状态：idle/exercising/resting/project-rest/paused/completed
- [ ] 3.2 实现基于 `performance.now()` + `requestAnimationFrame` 的精确倒计时逻辑
- [ ] 3.3 实现逐组实际时长采集 — 引擎层在每组开始时记录起始时间戳，结束/跳过时计算 `actual` 时长（毫秒），写入会话数据
- [ ] 3.4 实现自动顺序执行：锻炼 → 组间休息 → 下一组 → 项目间休息 → 下一项目
- [ ] 3.5 实现暂停/恢复功能，包含 `visibilitychange` 事件的时间校准
- [ ] 3.6 实现跳过当前组/休息的功能，正确推进状态机
- [ ] 3.7 实现提前结束锻炼功能，保存已完成部分的数据
- [ ] 3.8 实现锻炼状态快照模块（`timer/snapshot.ts`）— 每次状态变更时将当前进度写入 localStorage
- [ ] 3.9 实现页面加载时的快照检测与恢复提示 — 检测未完结快照，提供「继续」或「放弃」选项
- [ ] 3.10 实现快照清理 — 锻炼正常结束或用户放弃恢复时清除 localStorage 快照
- [ ] 3.11 实现放弃恢复时保存不完整记录 — 用户选择「放弃」时，将快照中已完成的组/项目作为一次不完整会话记录保存到 IndexedDB，标记 `status: 'incomplete'`，然后清除快照

## 4. 声音与振动提示

- [ ] 4.1 实现 Web Audio API 提示音模块（`timer/audio.ts`）— 延迟初始化，`init()` 必须在用户手势事件中调用
- [ ] 4.2 实现用户手势 AudioContext 解锁 — 在「开始锻炼」按钮点击事件中调用 `audio.init()`，播放静音短音解锁 iOS Safari 音频通道
- [ ] 4.3 实现状态切换提示（锻炼开始 1 声、锻炼结束 2 声）
- [ ] 4.4 实现最后 3 秒倒计时节拍提示音
- [ ] 4.5 集成 `navigator.vibrate()` 振动反馈（作为声音的补充）

## 5. 页面视图 — 首页

- [ ] 5.1 实现首页视图（`views/home.ts`）— 显示「开始锻炼」主按钮
- [ ] 5.2 显示连续锻炼天数（streak）统计和历史最长连续天数（从 `metadata` store 读取 `maxStreak`）
- [ ] 5.3 显示今日锻炼状态（已完成/未完成）
- [ ] 5.4 添加导航到配置页和历史记录页的入口

## 6. 页面视图 — 锻炼执行页

- [ ] 6.1 实现锻炼执行页视图（`views/workout.ts`）— 全屏计时器显示
- [ ] 6.2 实现计时器显示组件（`components/timer-display.ts`）— 大字倒计时、当前项目名、组号/总组数、当前阶段标签
- [ ] 6.3 实现进度条组件（`components/progress-bar.ts`）— 显示整体锻炼进度
- [ ] 6.4 实现操作按钮区域：暂停/恢复、跳过、结束锻炼
- [ ] 6.5 集成 Screen Wake Lock API 保持屏幕常亮

## 7. 页面视图 — 完成统计页

- [ ] 7.1 实现完成统计页视图（`views/complete.ts`）— 锻炼结束后的统计展示
- [ ] 7.2 实现完成率环形图（Chart.js doughnut chart）
- [ ] 7.3 实现各项目完成情况柱状图（完成组数 vs 总组数）
- [ ] 7.4 显示总用时、跳过组数等关键指标
- [ ] 7.5 实现与上次锻炼的对比显示（完成率变化、时长变化，箭头标记升降）
- [ ] 7.6 实现可选备注输入框，保存备注到锻炼记录

## 8. 页面视图 — 历史记录页

- [ ] 8.1 实现历史记录页视图（`views/history.ts`）— 包含日历视图和趋势图
- [ ] 8.2 实现日历视图组件 — 月历展示，颜色标记锻炼完成情况（绿/黄/灰）
- [ ] 8.3 实现点击日期查看锻炼详情
- [ ] 8.4 实现周趋势折线图（最近 7 天完成率，Chart.js line chart）
- [ ] 8.5 实现月趋势折线图（最近 30 天完成率）
- [ ] 8.6 实现周平均对比（当周 vs 上周平均完成率）

## 9. 页面视图 — 配置管理页

- [ ] 9.1 实现配置页视图（`views/config.ts`）— 锻炼计划编辑界面
- [ ] 9.2 实现项目参数编辑表单（名称、每组时长、休息时长、组数）
- [ ] 9.3 实现项目排序功能（拖拽或上下移动按钮）
- [ ] 9.4 实现添加新项目和删除项目功能
- [ ] 9.5 实现项目间休息时长的全局配置
- [ ] 9.6 实现表单参数验证（正数校验、必填校验）
- [ ] 9.7 集成数据导出/导入按钮

## 10. PWA 支持

- [ ] 10.1 创建 `manifest.json`（应用名称、图标、主题色、启动 URL）
- [ ] 10.2 创建 Service Worker — 缓存核心资源实现离线访问
- [ ] 10.3 配置 Vite 的 PWA 插件（`vite-plugin-pwa`）自动生成 SW

## 11. 集成测试与优化

- [ ] 11.1 端到端测试：完整锻炼流程（启动 → 执行 → 完成 → 查看统计）
- [ ] 11.2 验证计时器精度（后台切换后时间校准）
- [ ] 11.3 验证数据持久化（刷新页面后数据不丢失）
- [ ] 11.4 验证崩溃恢复：锻炼中刷新页面 → 恢复提示 → 选择继续 → 进度衔接正确
- [ ] 11.5 验证 iOS Safari 音频：点击开始后提示音正常播放
- [ ] 11.6 验证跨夜场景：23:55 开始锻炼 → 00:05 结束 → 记录归属正确日期、streak 不中断
- [ ] 11.7 移动端触摸体验优化（按钮大小、防误触、手势）
- [ ] 11.8 Lighthouse PWA 审计，确保评分达标
